[toolchain]
anchor_version = "0.30.1"

[features]
resolution = true
skip-lint = false

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "devnet"
wallet = "~/.config/solana/id.json"

[programs.devnet]
tap_duel = "TapDue1XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
